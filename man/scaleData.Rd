% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_process.R
\name{scaleData}
\alias{scaleData}
\title{Standardize Data by Cohorts}
\usage{
scaleData(data, cohort = NULL, centerFlags = NULL, scaleFlags = NULL)
}
\arguments{
\item{data}{A data frame or matrix where rows represent samples and columns represent genes.
The dataset to be standardized.}

\item{cohort}{An optional vector indicating the cohort (group) assignment for each sample.
If not provided (NULL), all samples are assumed to belong to a single cohort.}

\item{centerFlags}{Logical value(s) or named logical vector. Determines whether to center
the gene values to have a mean of zero within each cohort. Defaults to NULL (no centering).
If a single TRUE/FALSE value is provided, it applies to all cohorts. If a named vector is
provided, the names must correspond to cohort names.}

\item{scaleFlags}{Logical value(s) or named logical vector. Determines whether to scale
the gene values to have a standard deviation of one within each cohort. Defaults to NULL
(no scaling). The usage is similar to centerFlags.}
}
\value{
A standardized data matrix with rows reordered to match the original sample names.
}
\description{
Standardize Data by Cohorts
}
\examples{
data <- matrix(rnorm(100), nrow = 10, ncol = 10)
cohort <- rep(c("A", "B"), each = 5)
centerFlags <- c("A" = TRUE, "B" = FALSE)
scaleFlags <- TRUE
scaledData <- scaleData(data, cohort, centerFlags, scaleFlags)
}
